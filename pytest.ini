[pytest]
addopts = -q --cov=backend --cov-report=term-missing
python_files = test_*.py
python_functions = test_*
norecursedirs = .git .venv __pycache__ .pytest_cache
# Exclude manual integration tests that require running server
testpaths = tests

